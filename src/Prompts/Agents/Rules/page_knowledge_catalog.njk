------------------------------
📚 PAGE KNOWLEDGE CATALOG
👉 Use this catalog to map test steps to specific Page Contexts and Workflows.
    1️⃣. Homepage
        📌 Description: The main landing page of the travel booking application. Includes search bars, date pickers, and destination inputs.
        📌 Triggers: Actions like "Go to homepage", "Search for a hotel", "Input destination", or "Select dates".
        📌 Files:
            Context: src/Pages/Contexts/homepage_context.njk
            Workflow: src/Pages/Workflows/homepage_workflow.njk
    2️⃣. Search Results Page
        📌 Description: The page displaying a list of hotels/properties after a search is executed. Includes filters, sorting, and property cards.
        📌 Triggers: Actions like "Filter by price", "Sort by rating", "Select the first hotel in the list", or "View property details".
        📌 Files:
            Context: src/Pages/Contexts/searchpage_context.njk
            Workflow: src/Pages/Workflows/search_workflow.njk
    3️⃣. Property Details / Room Selection (Example - add if needed)
        📌 Description: The page showing specific room types, facilities, and the "Book Now" buttons for a single hotel.
        📌 Triggers: Actions like "Select a Deluxe Room", "Check room facilities", or "Click Reserve".
        📌 Files:
            Context: src/Pages/Contexts/property_context.njk
            Workflow: src/Pages/Workflows/booking_workflow.njk
------------------------------
📋 MAPPING INSTRUCTIONS FOR AGENT:
    📌 Analyze Intent: Read the "action" and "expectedResults" of the current test step.
    📌 Match Trigger: Determine which page the user is currently on or navigating to.
    📌 Check Previous State:
        If the current step is on the same page as the previous step, return empty arrays [] for both fields to save tokens.
        If this is the first step or a new page, provide the full paths from the catalog above.
    📌 Format: Return the file paths exactly as listed in the "Files" section of each catalog entry.
------------------------------
{{ AdditionalContexts }}
------------------------------

